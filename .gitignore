# Rust
target/
target-private/
**/*.rs.bk

# wasm-pack output
**/pkg/

# Node / TS
node_modules/
dist/
.pnpm-store/

# Playwright e2e output
test-results/
playwright-report/

# Tooling
tmp/
.vscode/
.idea/
*.log

# Local toolchain installs (portable JDK, veraPDF, chromedriver)
.tools/
.driver/

# Diagnostic one-shots
find-driver.mjs
ts/probe-renderer.mjs

# Per-user scheduled task locks
.claude/scheduled_tasks.lock

# Visual-diff golden backups (kept locally for human inspection only)
tools/visual-diff/golden.*.bak/

# tools/telemetry-sink runtime output (Issue #86, D5.7) — the dev/e2e
# receiver's on-disk JSONL, never committed.
tools/telemetry-sink/*.jsonl

# OS
.DS_Store
Thumbs.db

# Claude Code per-user overrides (settings.json is committed; .local.json is personal)
.claude/settings.local.json
.claude/.cache/

# Private cargo cache for merge gates on main (never commit)
target-main/
