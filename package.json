{
  "name": "next-gen-editor-monorepo",
  "version": "0.5.0-beta.3",
  "private": true,
  "workspaces": [
    "packages/*",
    "ts",
    "tools/*"
  ],
  "scripts": {
    "dev": "pnpm --filter next-gen-editor-ui dev",
    "build": "pnpm -r --filter './packages/*' build && pnpm --filter next-gen-editor-ui build",
    "tsc": "pnpm -r tsc"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
