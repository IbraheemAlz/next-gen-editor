[package]
name = "layout"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Paragraph + table + page box layout engine."

[dependencies]
engine = { path = "../engine" }
text-pipeline = { path = "../text-pipeline" }
